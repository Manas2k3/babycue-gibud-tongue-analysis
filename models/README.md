# Models

This folder contains model artifacts used by the pipeline. The large binary files (e.g., `.pth`, `.pkl`) are intentionally ignored by git to keep the repository lightweight.

If you need the weights, place them in this directory or download them from the shared link in `readme.txt`.
